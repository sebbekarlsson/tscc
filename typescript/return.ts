function test3(arg:number): number {
    return (arg + arg) * 2;
}

function test(arg:number): number {
    return arg;
}

function test2(arg:number): number {
    return arg + arg;
}

function test5(arg:number): number {
    return 2 + (arg + arg) * 2;
}